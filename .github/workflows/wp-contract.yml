# Peanut — WordPress REST contract tests (net 7) on a REAL WordPress.
# Boots a real WP against a MySQL service and runs the real-WP contract suite
# (tests/ContractWp via phpunit.contract-wp.xml) — separate from the mock-based
# unit suites so it can never silently fall back to mocks.
#
# Prereqs in the plugin (see .peanut/wp-harness / docs wp-harness/README.md):
#   - wp-phpunit/wp-phpunit + yoast/phpunit-polyfills in require-dev
#   - phpunit ^9.6 (WP's test lib calls a method PHPUnit 10 removed)
#   - tests/ContractWp/bootstrap.php defines PLUGIN_MAIN_FILE then requires the shared bootstrap-wp.php
#   - phpunit.contract-wp.xml points at tests/ContractWp with that bootstrap
name: wp-contract

on:
  push: { branches: [ main ] }
  pull_request:

jobs:
  wp-contract:
    runs-on: ubuntu-latest
    services:
      mysql:
        image: mysql:8
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: wordpress_test
        ports: [ "3306:3306" ]
        options: >-
          --health-cmd="mysqladmin ping -proot" --health-interval=5s
          --health-timeout=5s --health-retries=10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: "8.3"
          extensions: mysqli
          tools: composer
      - run: composer update --no-interaction --prefer-dist

      - name: Verify vendored WP harness
        env:
          WP_HARNESS_SHA256: "e27b32255e0d61d7e5da05d082ba4b9d582b0a6adf676d67854d09b9dece14b6"
          WP_BOOTSTRAP_SHA256: "bf6bcf4a73f79d9ffb88381e5d334fd97e5876d771da5fce037acc46dd761619"
          WP_LIFECYCLE_GUARD_SHA256: "19d9a22a3e680ceb4c1ad6304285148d0d2aabe58c2a65f83ccbd1f36bd05587"
        run: |
          set -euo pipefail
          printf '%s  %s\n' "$WP_HARNESS_SHA256" .peanut/wp-harness/install-wp-tests.sh \
            | sha256sum --check --strict
          printf '%s  %s\n' "$WP_BOOTSTRAP_SHA256" .peanut/wp-harness/bootstrap-wp.php \
            | sha256sum --check --strict
          printf '%s  %s\n' "$WP_LIFECYCLE_GUARD_SHA256" .peanut/wp-harness/lifecycle-hook-guard.php \
            | sha256sum --check --strict
          install -m 0755 .peanut/wp-harness/install-wp-tests.sh /tmp/install-wp-tests.sh
          echo "WP harness SHA-256: $WP_HARNESS_SHA256"
          # Regression test for the harness itself (GAP-03): proven to fail on the
          # unfixed installer, which silently paired trunk's test library with
          # pinned core. Runs offline; curl is stubbed.
          bash .peanut/wp-harness/tests/install-wp-tests.test.sh

      - name: Install WordPress test suite
        env:
          WP_TESTS_DIR: /tmp/wordpress-tests-lib
          WP_CORE_DIR: /tmp/wordpress
        run: /tmp/install-wp-tests.sh wordpress_test root root 127.0.0.1

      - name: REST contract tests (net 7, real WordPress)
        env:
          WP_TESTS_DIR: /tmp/wordpress-tests-lib
          WP_CORE_DIR: /tmp/wordpress
        run: vendor/bin/phpunit -c phpunit.contract-wp.xml

  # Additive rather than a matrix conversion: branch protection requires the
  # existing exact `wp-contract` context. This second job proves the verified
  # central-host database family without renaming or weakening that gate.
  #
  # Everything except the database service is copied from the job above on
  # purpose — same PHP, same runner, same suite — so a failure here means the
  # engine, not an unrelated difference.
  wp-contract-mariadb:
    name: wp-contract-mariadb
    runs-on: ubuntu-latest
    services:
      mariadb:
        image: mariadb:10.6
        env:
          MARIADB_ROOT_PASSWORD: root
          MARIADB_DATABASE: wordpress_test
        ports: [ "3306:3306" ]
        options: >-
          --health-cmd="mysqladmin ping -proot" --health-interval=5s
          --health-timeout=5s --health-retries=10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: shivammathur/setup-php@f3e473d116dcccaddc5834248c87452386958240 # 2.37.2
        with:
          php-version: "8.3"
          extensions: mysqli
          tools: composer
      - run: composer update --no-interaction --prefer-dist

      - name: Verify vendored WP harness
        env:
          WP_HARNESS_SHA256: "e27b32255e0d61d7e5da05d082ba4b9d582b0a6adf676d67854d09b9dece14b6"
          WP_BOOTSTRAP_SHA256: "bf6bcf4a73f79d9ffb88381e5d334fd97e5876d771da5fce037acc46dd761619"
          WP_LIFECYCLE_GUARD_SHA256: "19d9a22a3e680ceb4c1ad6304285148d0d2aabe58c2a65f83ccbd1f36bd05587"
        run: |
          set -euo pipefail
          printf '%s  %s\n' "$WP_HARNESS_SHA256" .peanut/wp-harness/install-wp-tests.sh \
            | sha256sum --check --strict
          printf '%s  %s\n' "$WP_BOOTSTRAP_SHA256" .peanut/wp-harness/bootstrap-wp.php \
            | sha256sum --check --strict
          printf '%s  %s\n' "$WP_LIFECYCLE_GUARD_SHA256" .peanut/wp-harness/lifecycle-hook-guard.php \
            | sha256sum --check --strict
          install -m 0755 .peanut/wp-harness/install-wp-tests.sh /tmp/install-wp-tests.sh
          echo "WP harness SHA-256: $WP_HARNESS_SHA256"
          # Regression test for the harness itself (GAP-03): proven to fail on the
          # unfixed installer, which silently paired trunk's test library with
          # pinned core. Runs offline; curl is stubbed.
          bash .peanut/wp-harness/tests/install-wp-tests.test.sh

      - name: Install WordPress test suite
        env:
          WP_TESTS_DIR: /tmp/wordpress-tests-lib
          WP_CORE_DIR: /tmp/wordpress
        run: /tmp/install-wp-tests.sh wordpress_test root root 127.0.0.1

      - name: REST contract tests (net 7, real WordPress on MariaDB 10.6)
        env:
          WP_TESTS_DIR: /tmp/wordpress-tests-lib
          WP_CORE_DIR: /tmp/wordpress
        run: vendor/bin/phpunit -c phpunit.contract-wp.xml
